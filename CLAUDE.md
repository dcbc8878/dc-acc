# CRM Integration Project — ห้องกลาง (Central Hub)

> **กฎข้อแรก: ก่อนเริ่มงานทุกครั้งในโปรเปรกต์นี้ ต้องอ่านไฟล์นี้ทั้งหมดก่อน** โดยเฉพาะหัวข้อ
> "สถานะล่าสุด" และ "กฎการทำงาน" ด้านล่าง เพราะเนื้อหาจะถูกอัปเดตเรื่อยๆ ระหว่างทาง
> ห้ามสมมติว่าข้อมูลที่เคยรู้ยังเป็นปัจจุบันอยู่ — ให้เช็คไฟล์นี้ใหม่ทุกครั้ง

## เป้าหมายโปรเจกต์

รวม **3 ส่วนงาน** (โค้ด/สคริปต์ที่มีอยู่แล้วคนละที่) ให้กลายเป็น **เว็บแอปเดียว** แล้ว
**deploy** ขึ้นใช้งานจริง โดยเว็บแอปต้อง **เชื่อมต่อกับ Google Sheets** ได้

Repo/branch นี้ (`claude/integrated-app-google-sheets-jca7dd`) คือ **ห้องเดียว** ที่ผู้ใช้ตั้งใจให้ทำทุกอย่าง — **ไม่ใช้ห้อง/เซสชันแยกอีกต่อไป** งานส่วนไหนที่ต้องแยกทำ ให้ใช้ **sub agent** (ผ่าน Agent tool) ที่รันจากห้องนี้เท่านั้น แล้วรายงานผลกลับมาที่ห้องนี้โดยตรง

> ประวัติ: ช่วงแรกเคยลองแยกงานเป็นห้อง/เซสชันอื่น (ห้อง "น้องบัญชี", ห้อง "DC.CRM") แต่ผู้ใช้ยกเลิกแนวทางนี้แล้วเมื่อ 2026-08-18 (ลบ branch ที่เกี่ยวข้องทั้งหมด) เปลี่ยนมาเป็นทำทุกอย่างผ่านห้องนี้ห้องเดียวแทน — **ห้ามสร้างห้อง/เซสชันแยกอีก ใช้ sub agent เท่านั้น**

## กฎการทำงาน (Working Rules)

1. **อ่านไฟล์นี้ทั้งหมดก่อนเริ่มงานทุกครั้ง** เช็คว่ามีหัวข้อ/กฎใหม่ที่ถูกเพิ่มเข้ามาหรือไม่
2. **ห้ามเขียนความลับ (token/API key/credentials) ลงในโค้ดตรงๆ** — ต้องใช้ environment variable หรือ secret manager ของ hosting ที่เลือกใช้เสมอ (ดูหมายเหตุเรื่อง LINE token ในส่วนที่ 1 ด้านล่าง เป็นตัวอย่างสิ่งที่ต้องแก้ตอน integrate)
3. เมื่อได้ข้อมูล/โค้ดของส่วนที่ 2 และ 3 มาแล้ว ให้อัปเดตหัวข้อ "สถานะล่าสุด" ในไฟล์นี้ก่อนเริ่มออกแบบ/เขียนสถาปัตยกรรมรวม
4. อย่าเพิ่งลงมือสร้างสถาปัตยกรรมรวม (merge stack, ตัดสินใจ hosting จริงจัง) จนกว่าจะรู้ครบทั้ง 3 ส่วนว่าแต่ละส่วนเขียนด้วยอะไร ทำงานยังไง
7. **ทำทุกอย่างในห้องนี้ห้องเดียว** ห้ามสร้างห้อง/เซสชันแยกใหม่ (เช่นผ่าน create_session) — งานที่ต้องแยกทำหรือขนาน ให้ใช้ sub agent (Agent tool) ที่รันอยู่ภายในห้องนี้เท่านั้น แล้วให้รายงานผลกลับมาที่นี่ ไม่ใช่ไปสร้างประวัติ/branch ใหม่แยกออกไป
5. **ผู้ใช้ไม่มีความรู้เรื่องโปรแกรม** — ให้ลงมือทำเองให้ได้มากที่สุด (เขียนโค้ด, สั่งคำสั่ง, ตั้งค่าที่ทำผ่าน CLI/API ได้) ห้ามโยนงานที่ทำเองได้ไปให้ผู้ใช้ทำ
6. ถ้ามีขั้นตอนที่ทำเองไม่ได้จริงๆ (ต้องให้ผู้ใช้กดในเว็บ/คอนโซลเอง เช่น สมัครบัญชี, ยืนยันตัวตน, จ่ายเงิน, กด consent) **ต้องบอกเป็นขั้นตอนละเอียดทีละสเต็ป** ว่าต้องเข้าเว็บไหน กดตรงไหน ใส่อะไร ห้ามบอกลอยๆ ว่า "ไปตั้งค่า X เอง"

## กฎการตอบข้อความ (Communication Rules)

- **ตอบเป็นภาษาไทยเท่านั้นทุกครั้ง**
- **ตอบแบบสรุปสั้นๆ เท่านั้น** ไม่ต้องลงรายละเอียดทางเทคนิคยืดยาว เว้นแต่เป็นขั้นตอนที่ต้องให้ผู้ใช้ทำเอง (ตามกฎข้อ 6 ด้านบน) ให้ละเอียดได้เฉพาะส่วนนั้น

## สถานะล่าสุด (Latest Status)

_อัปเดตล่าสุด: 2026-08-18_

> **⚠️ อัปเดตใหญ่ 2026-08-18 (รอบบ่าย):** นิยาม "3 ส่วน" เปลี่ยนไปจากที่บันทึกไว้ตอนเช้าทั้งหมด (ตอนเช้าคือ LINE bot / ห้องน้องบัญชี / ห้อง DC.CRM — ข้อมูลนั้นเลิกใช้แล้ว) นิยามล่าสุดคือฟังก์ชัน 3 อย่างของสำนักงานบัญชี: **CRM / Calendar / AI chat bot** ตามด้านล่างนี้ และเจอว่ามี**เว็บไซต์จริงที่ deploy ใช้งานอยู่แล้ว** (`www.dcbc.co.th`) — ทั้งโปรเจกต์เปลี่ยนจาก "สร้างใหม่" เป็น "เอาของที่มีอยู่แล้วมาต่อกันในเว็บจริง"

### ส่วนที่ 1 — CRM: มีอยู่แล้ว เป็นระบบ production จริง ✅
- **Live ที่ `www.dcbc.co.th/crm/`** — repo `dcbc8878/web` (GitHub Pages, auto-deploy ผ่าน `.github/workflows/deploy.yml` ทุก push เข้า `main`/`claude/dcbc-website-setup-1hs9tw` + ทุกชั่วโมง)
- Backend จริง: **Supabase project "Web"** (ref `dqegkyobclqqichhnxfm`) — ตารางขึ้นต้นด้วย `crm_*`
- ไฟล์เดียว `crm/index.html` (เวอร์ชัน production ปัจจุบัน 3,861 บรรทัด)
- ผู้ใช้อัปโหลด `crmcomplete.zip` มาให้ (2026-08-18) — เป็น**เวอร์ชันใหม่กว่า** (4,539 บรรทัด, README บอกว่าคัดลอกมาจาก branch `claude/crm` ของ `dcbc8878/ApplicationTest`) มีฟีเจอร์เพิ่ม: สมัครพนักงานเอง+อนุมัติ, ทีม (`crm_teams`), บริษัทภายใน, ตั้งค่าโปรแกรม, จำกัดสิทธิ์เห็นลูกค้าตามทีม — **ยังไม่ได้ deploy ขึ้นเว็บจริง** ไฟล์อยู่ที่ `/tmp/claude-0/-home-user-CRM/831e28e5-2ec6-5b68-9cea-61f0ff132b63/scratchpad/crmcomplete/`
- ⚠️ ก่อนเอาเวอร์ชันใหม่ขึ้นจริง ต้องเช็คว่า schema/migration ที่เวอร์ชันใหม่ต้องการ (`supabase-crm-*-migration.sql` ในซิป มีมากกว่าที่ repo `dcbc8878/web` มี — เช่น teams, staff-requests, program-settings) apply ไปที่ฐานข้อมูลจริงหรือยัง

### ส่วนที่ 2 — Calendar: มีโค้ดแล้ว แต่ยังไม่ deploy ขึ้นเว็บจริง ⏳
- อยู่ใน repo `dcbc8878/ApplicationTest` branch `claude/read-rules-0kac0l` ไฟล์ `dcbc-calendar.html` (1,530 บรรทัด) — ต่อ Supabase project "Web" เดียวกัน (ตาราง `cal_*`)
- **ยังไม่ยืนยัน 100% กับผู้ใช้ว่านี่คือส่วนที่ 2 จริง** (ตอนถามรวม 3 branch ผู้ใช้ตอบ "ไม่ใช่เลย" แต่หลังจากนั้นอัปโหลด CRM ที่ตรงกับอีก branch หนึ่งในชุดเดียวกันมาให้ — น่าจะตอบ "ไม่ใช่" เพราะ branch AI chat ผิด ไม่ใช่เพราะ CRM/Calendar ผิด แต่ต้อง**คอนเฟิร์มกับผู้ใช้อีกครั้งก่อนใช้จริง**)
- ไม่มีอยู่ใน `dcbc8878/web` (เว็บจริง) เลย — ต้องเพิ่มเป็นหน้าใหม่ (เช่น `/calendar`) ถ้ายืนยันว่าใช่

### ส่วนที่ 3 — AI chat bot: ต่อกับระบบ "น้องบัญชี" เดิม ⏳
- ผู้ใช้ยืนยันว่า "ตอนนี้ใช้งานผ่าน Google Sheet" หมายถึงระบบ **BOT_น้องบัญชี** (LINE OA Bot เดิมที่เจอใน Google Drive ตั้งแต่ตอนเช้า — ดูรายละเอียดฟังก์ชัน/sheet ด้านล่าง หัวข้อ "ระบบเดิมที่เกี่ยวข้อง")
- ต้องการ: เก็บข้อมูลได้เยอะ (ข้อมูลใน Google Sheets ตอนนี้เริ่มเยอะ) **แต่ยังต้องโต้ตอบได้ในหน้าเว็บ** (ไม่ใช่แค่ผ่าน LINE) — ยังไม่ได้ลงรายละเอียดว่าจะ build หน้า chat ใหม่ หรือย้ายข้อมูลไป Supabase ด้วย หรือให้ AI อ่าน Sheet ตรงๆ
- โปรเจกต์ `dcbc8878/ApplicationTest` branch `claude/ai-chat-section-26htkq` (aichat) **ผู้ใช้ยืนยันว่าไม่เกี่ยวกับส่วนนี้** (คนละเรื่อง) — อย่าเอามาใช้/อย่าอ้างอิงอีก

## ⚠️ พบระบบที่ 4 ที่ยังไม่รู้ตัว — อาจเป็นส่วนที่ 3 (AI chat) ที่ทำไว้แล้วจริงๆ

สั่ง sub agent ไปตรวจสอบว่าน้องบัญชีเก็บข้อมูลจากห้องไลน์ได้แค่ไหน (2026-08-18) พบว่า:

- **BOT_น้องบัญชี** (โค้ดที่มีอยู่แล้ว) ดักได้แค่ `/setup` กับไฟล์/รูปเท่านั้น — **ข้อความแชทธรรมดาไม่ถูกบันทึกเลย** (ตกหล่นทุกข้อความที่ไม่ตรงเงื่อนไข)
- **"Archive"** (Apps Script อีกตัวที่เจอ) เป็นแค่ draft ที่ยังไม่ติดตั้งจริง เรียกฟังก์ชันที่ไม่มีอยู่จริงในโค้ด — ยังไม่ได้ใช้งาน
- **แต่ Sheet "LINE_Archive_2026" มีข้อมูลจริงเต็มเยอะมาก ปรับปรุงถึงวันนี้ (18 ส.ค.)** — แท็บ `_Queue` (498 event ดิบ), `Messages` (123 ข้อความจริงพร้อมชื่อ/กลุ่ม — ดักข้อความธรรมดาได้จริง ไม่ใช่แค่ /setup), `Events`, `Members` (25 คน), และที่สำคัญ: **มี AI Q&A ทำงานอยู่แล้วจริง** (คำสั่ง `/ai` `/today` `/alert` ยิงไป Vertex AI/Gemini พร้อม log token/cost, ใช้งานจริงถึงวันนี้), มี **OCR เอกสาร** (136 ไฟล์ ดึง doc_type/tax_id/VAT), Tasks, Staff roster, ตาราง Fees/billing (408 แถว)
- **หาต้นฉบับโค้ดของระบบนี้ไม่เจอ** ในไฟล์ Apps Script ที่เห็นใน Drive (มีแค่ BOT_น้องบัญชี กับ Archive) — น่าจะเป็น container-bound script ผูกกับตัว Sheet โดยตรง (เปิดผ่าน Extensions > Apps Script ในชีตนั้นเลย ไม่ใช่ไฟล์แยกใน Drive) คาดว่าเป็นผลงานจากห้อง "ห้องน้องบัญชี" ที่ถูกลบ session ไปแล้วก่อนหน้านี้
- **นัยสำคัญ:** ส่วนที่ 3 (AI chat) อาจจะ**มีอยู่แล้วเกือบสมบูรณ์** ไม่ต้องสร้างใหม่ทั้งหมด — แค่ต้องหาโค้ดต้นฉบับให้เจอ (ต้องเปิด Sheet "LINE_Archive_2026" ตรงๆ ใน Google Sheets UI แล้วดู Extensions > Apps Script ถึงจะเห็น — เครื่องมือ Drive API ที่ห้องนี้ใช้ไม่เห็น container-bound script)

## ระบบเดิมที่เกี่ยวข้อง (ไม่ใช่ 1 ใน 3 ส่วนโดยตรง แต่ส่วนที่ 3 ต้องใช้ข้อมูลนี้)
**BOT_น้องบัญชี** — Google Apps Script (LINE OA Bot) สำหรับธุรกิจรับทำบัญชี "เด็กชายบัญชี"
- พบใน Google Drive ที่เชื่อมต่อกับเซสชันนี้ (ไฟล์ประเภท Apps Script ชื่อ "BOT_น้องบัญชี")
- ฟังก์ชันหลักเท่าที่สำรวจ: `mainTrigger`/`sendScheduledMessages` (ส่งข้อความแจ้งเตือนตามตาราง), `doPost` (webhook LINE), `handleSetupCommand` (`/setup`), `handleFileArchiving` (เก็บไฟล์ลูกค้าใน Drive), `pushMessage`/`replyMessage`
- ใช้ Google Sheet เป็นฐานข้อมูล มีแท็บ: `Message`, `Group`, `Files` + Sheet คู่กันชื่อ **"LINE_Archive_2026"** (แท็บ `Message`, `Notes`, `Reminders`)
- ⚠️ โค้ดฝัง LINE Channel Access Token และ Sheet/Folder ID ไว้ตรงๆ — ตอน integrate ต้องย้ายเป็น secret

## สถาปัตยกรรมจริง (ค้นพบแล้ว ไม่ใช่แค่คำแนะนำอีกต่อไป)

- **Hosting: มีอยู่แล้ว** — `dcbc8878/web` repo, GitHub Pages, โดเมน `www.dcbc.co.th`, auto-deploy ทุก push (ฟรี, ไม่มีวันหมดอายุ) → **ไม่ต้องหาที่ deploy ใหม่**
- **Backend หลัก: Supabase project "Web"** (ref `dqegkyobclqqichhnxfm`) — CRM ใช้อยู่แล้ว, Calendar (ถ้าใช่ส่วนที่ 2) ก็ต่อ project เดียวกัน, login ร่วมกันผ่าน `auth.users`
- **Google Sheets** ยังจำเป็นสำหรับส่วนที่ 3 เท่านั้น (ข้อมูลเดิมของน้องบัญชี) — ไม่ใช่ backend หลักของทั้งระบบอีกต่อไป
- **งานที่เหลือ:** ทำหน้าเว็บให้ผู้ใช้เข้าถึงทั้ง 3 ส่วนได้จากที่เดียว (เช่น เมนู/แท็บใน `dcbc8878/web`), เพิ่ม Calendar เข้าไปเป็นหน้าใหม่, ต่อ AI chat ให้อ่าน/เขียนข้อมูลจาก Sheet ของน้องบัญชีได้ผ่านหน้าเว็บ

## คำตอบจากผู้ใช้ (2026-08-18 บ่าย) — คลี่คลายจุดค้างด้านบนแล้ว

1. **ที่อยู่เว็บ:** ผู้ใช้จะ **เปลี่ยนที่อยู่เว็บใหม่** (ไม่ใช้ `www.dcbc.co.th`/repo `dcbc8878/web` เดิมสำหรับงานนี้) — ยังไม่รู้ที่อยู่ใหม่คืออะไร/อยู่ repo ไหน **ยังไม่ได้ push ไปที่ dcbc8878/web และจะไม่ push ไปที่นั่นจนกว่าจะรู้ที่อยู่ใหม่** งานตอนนี้ให้ทำเป็น "เตรียมของ" ในห้องนี้ไปก่อน (โค้ด/schema/preview) แล้วค่อยย้ายไปที่ปลายทางจริงทีหลัง
2. **Calendar:** ยืนยันแล้ว branch `claude/read-rules-0kac0l` (`dcbc-calendar.html`) ใน `dcbc8878/applicationtest` คือส่วนที่ 2 จริง ✅
3. **CRM เวอร์ชันใหม่ + พนักงาน:** ไม่ใช่แค่ "อัปเป็นเวอร์ชันใหม่เฉยๆ" — ผู้ใช้ต้องการ **แยกระบบ "พนักงาน" ออกมาเป็นส่วนกลาง ล็อกอินครั้งเดียวใช้ได้ทุกส่วน** (CRM/Calendar/AI chat) แทนที่จะผูกกับ CRM อย่างเดียว

## แผนแยกระบบ "พนักงาน" เป็นส่วนกลาง (Shared staff/auth module)

สำรวจ schema ในซิป CRM ใหม่แล้วพบว่าเรื่องพนักงานผูกกับ CRM ลึกมาก:
- ตาราง `crm_staff` (โปรไฟล์พนักงาน: role 6 ระดับ, ชื่อเล่น, รหัสพนักงาน, is_active), `crm_teams`, `crm_staff_requests` (สมัคร+อนุมัติ)
- ฟังก์ชัน `crm_get_role()` (ใช้ตรวจสิทธิ์แทบทุก RLS policy ในระบบ) + Edge Function `admin-staff` (create/reset/self_update/approve_request)
- มี **17 ไฟล์ migration** ที่แตะตาราง `crm_staff` สะสมมาเรื่อยๆ (schema ซับซ้อน ไม่ใช่ตารางเดียวโดดๆ)
- Login จริงๆ ใช้ Supabase `auth.users` ร่วมกันอยู่แล้วทุกแอพ (เพราะ project เดียวกัน) — สิ่งที่ **ยังไม่ share** คือ "โปรไฟล์พนักงาน" (ชื่อเล่น/รหัส/ทีม/role/สถานะอนุมัติ) ซึ่งตอนนี้ผูกกับ namespace `crm_*` เท่านั้น

**แนวทาง:** ย้าย/rename `crm_staff` → `crm_teams` → `crm_staff_requests` (+ ฟังก์ชัน/RLS ที่เกี่ยวข้อง) ออกมาเป็น namespace กลางที่ไม่ผูกกับแอพไหนแอพหนึ่ง (เช่น `org_staff`/`org_teams`/`org_staff_requests`) แล้วให้ CRM, Calendar, AI chat อ้างอิงตารางชุดนี้ร่วมกัน — เท่ากับ login ครั้งเดียว เห็นสิทธิ์/ทีม/role เดียวกันทุกส่วน

⚠️ **แก้ไขความเข้าใจผิด (เดิมเขียนว่า production ยังไม่มี crm_staff — ผิด):** เช็ค repo `dcbc8878/web` จริงแล้วพบว่า production **มี `crm_staff` พร้อมข้อมูลพนักงานจริงอยู่แล้ว** (มี migration 5-level role, staff_code, birthdate, onboarding ครบ) ที่ยังไม่มีคือ nickname/teams/staff_requests/staff_private เท่านั้น — เพราะงั้นการ rename `crm_staff` → `org_staff` ใน production **จะกระทบข้อมูลพนักงานจริง** (แม้ `ALTER TABLE...RENAME` จะปลอดภัยและไม่ทำข้อมูล/FK/RLS หายก็ตาม) ต้องระวังเป็นพิเศษตอน apply จริง

ออกแบบ migration ไว้แล้วที่ไฟล์ `supabase-org-staff-shared-migration.sql` ในห้องนี้ — ใช้วิธี `RENAME` ล้วน (table + function) ซึ่ง Postgres รักษา FK/RLS/index เดิมให้อัตโนมัติ (ผูกกันด้วย OID ไม่ใช่ชื่อ) ทำให้ RLS ของ CRM เดิมอีกหลายสิบ policy ที่เรียก `crm_get_role()` ยังทำงานถูกต้องทันทีโดยไม่ต้องแก้ — สิ่งที่ต้องแก้เพิ่มคือฝั่ง JS/Edge Function ที่เรียกชื่อตารางแบบ string (`.from('crm_staff')` ฯลฯ) **แต่ห้าม apply migration นี้เข้า Supabase project "Web" (ref `dqegkyobclqqichhnxfm`) จริงจนกว่าจะรู้ปลายทางเว็บใหม่และ user คอนเฟิร์ม**

## ที่อยู่เว็บใหม่: repo `dcbc8878/dc-acc` → GitHub Pages

ผู้ใช้ยืนยันให้ใช้ repo ที่มีอยู่แล้วชื่อ **`dcbc8878/dc-acc`** เป็นปลายทางเว็บใหม่ (แทนที่จะสร้าง repo ใหม่ชื่อ `app` ที่เคยเสนอ — สร้าง repo ใหม่ไม่ได้เพราะสิทธิ์ GitHub integration ของห้องนี้ไม่พอ แต่ `dc-acc` มีอยู่แล้วและมีสิทธิ์ push ให้เรียบร้อย)

- Clone อยู่ที่ `/workspace/dc-acc` ในห้องนี้แล้ว (attach ผ่าน add_repo access:push)
- ตั้ง `.github/workflows/deploy.yml` แบบเดียวกับ `dcbc8878/web` (configure-pages + upload-pages-artifact + deploy-pages) — push ครั้งแรกไปแล้ว, workflow รันอัตโนมัติ, **ไม่ต้องให้ผู้ใช้ไปกดเปิด Pages เองใน Settings** (actions/configure-pages เปิดให้อัตโนมัติตอนรันครั้งแรก)
- ใส่ placeholder `index.html` ไปก่อน (หน้า "กำลังเตรียมระบบ") รอเนื้อหาจริง (hub + CRM/Calendar/AI chat) มาแทนที่
- URL ที่คาดว่าจะได้ (ยังไม่มีโดเมนของตัวเอง): `https://dcbc8878.github.io/dc-acc/`
- ยังไม่ได้ apply migration `org_staff` หรือย้ายโค้ด CRM/Calendar จริงมาลง repo นี้ — รอ preview ให้ผู้ใช้ดูก่อน

## ยืนยันแล้ว: "แบ่งห้อง" = ทีม (crm_teams)

ผู้ใช้ถามให้ตรวจสอบว่า migration พนักงานกลาง (org_staff) ยังคงความสามารถ "แบ่งห้อง...ตามเดิม" ได้ไหม — คอนเฟิร์มแล้วว่า "ห้อง" หมายถึง **ทีม** ใน CRM (`crm_teams`/`crm_team_members`, ที่ Senior เห็นเฉพาะลูกค้าทีมตัวเอง) ✅ **migration ที่ออกแบบไว้แล้วรองรับอยู่แล้ว** — ใช้วิธี `RENAME` เปลี่ยน `crm_teams`→`org_teams` ซึ่งรักษา FK/RLS/ข้อมูลทีมเดิมไว้ครบ ไม่ต้องแก้อะไรเพิ่ม

## ขั้นตอนถัดไป (Next Steps) — อัปเดต

1. ออกแบบ schema `org_staff`/`org_teams`/`org_staff_requests` (ต่อยอดจาก `crm_staff` เดิม แต่ตัดการผูกกับ `crm_*` namespace) — ทำเป็นไฟล์ SQL เตรียมไว้ ยังไม่ apply
2. ปรับโค้ด `crm/index.html` (จากซิปที่ได้มา) ให้เรียกตารางชุดใหม่แทน `crm_staff` เดิม
3. ปรับ `dcbc-calendar.html` ให้ต่อกับระบบพนักงานชุดเดียวกัน (ตรวจสิทธิ์/role เดียวกับ CRM)
4. ทำ Artifact preview ให้ผู้ใช้ดูภาพรวม (นำทางเดียว เข้าถึง CRM+Calendar ได้) ก่อน
5. รอผู้ใช้บอกที่อยู่เว็บใหม่ → ค่อย apply migration จริง + push ขึ้นจริง
6. ส่วนที่ 3 (AI chat ต่อ Sheet ของน้องบัญชี) ทำคู่ขนานหลังโครงหลักนิ่ง

## สถาปัตยกรรมการทำงาน: ห้องเดียว + sub agent (Single-room + sub-agent)

- ผู้ใช้ยืนยันแล้ว (2026-08-18) ว่าไม่ต้องการห้อง/เซสชันแยกอีก ให้ทำทุกอย่างผ่านห้องนี้ห้องเดียว
- เคยตั้งช่องทางสั่งงานข้ามห้อง (Routine ผูก persistent_session_id ไปห้อง "น้องบัญชี"/"DC.CRM") ไว้ช่วงสั้นๆ — **ลบทิ้งแล้ว** เพราะห้องปลายทางถูกลบไปแล้ว ไม่ต้องสร้างใหม่
- ต่อไปนี้งานที่ต้องแยกทำ/ขนานกัน ให้ใช้ **Agent tool (sub agent)** รันจากห้องนี้แทนเสมอ ไม่สร้าง session/branch แยกอีก
- repo อื่นที่เพิ่มเข้ามาดู (read-only) ในห้องนี้แล้ว: `dcbc8878/applicationtest` (`/workspace/applicationtest`), `dcbc8878/web` (`/workspace/dcbc8878/web`)

## ขั้นตอนถัดไป (Next Steps)

1. ถามผู้ใช้ 3 ข้อในหัวข้อ "จุดที่ต้องคุยกับผู้ใช้ก่อนลงมือ" ด้านบน
2. ถ้าได้สิทธิ์ push `dcbc8878/web` แล้ว — เพิ่ม Calendar เป็นหน้าใหม่, อัปเดต CRM เป็นเวอร์ชันใหม่ (ถ้าคอนเฟิร์ม), ต่อ AI chat กับ Sheet ของน้องบัญชี
3. ทำ Artifact preview ให้ผู้ใช้เปิดดูก่อน confirm ทุกครั้งก่อน push ขึ้นเว็บจริง (ธรรมเนียมเดิมของโปรเจกต์นี้ตามที่เจอใน CLAUDE.md ของ repo อื่น)
4. Build → Deploy → ทดสอบเชื่อมต่อ Google Sheets (ส่วน AI chat) แบบ end-to-end
